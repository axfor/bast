language: go

go:
  - "1.13.x"  

services:
  - redis-server

install:
    - go get -v .
    - go get -v golang.org/x/lint/golint

script:
    - diff <(gofmt -d .) <(echo -n)
    - go test -v ./...
    - go vet -x ./... 
    - go build ./...
    