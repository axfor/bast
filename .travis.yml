language: go

go:
  - "1.13.x"  
  
install:
    - go get -v .
    - go get -v golang.org/x/lint/golint

script:
    - diff <(gofmt -d .) <(echo -n)
    - go test -v ./...
    - go vet -x ./...
    - golint -set_exit_status ./... 
    - go build ./...
    